\section{Related Work}
\label{sec:related}

% Five subsections planned. Each cites the canonical papers.
%
% \subsection{Model cascading}
% FrugalGPT (Chen, Zaharia, Zou, \cite{frugalgpt}); CascadingLLMs and
% confidence-cascade variants. Try cheap first, escalate on confidence
% drop. 98% cost savings at 95%+ fidelity claims; 2% typical fidelity loss.
%
% \subsection{LLM routing}
% RouteLLM (\cite{routellm}) — preference-data-trained classifier;
% 85% cost reduction on MT-Bench at 95% of GPT-4 quality. Related:
% Hybrid LLM Routing (\cite{hybridllm}), AutoMix, Tryage
% (\cite{tryage}).
%
% \subsection{Confidence-based routing}
% Semantic entropy (\cite{semanticentropy}); hidden-state probes;
% verbalized-confidence baselines.
%
% \subsection{Plan caching for agentic systems}
% NeurIPS 2025 Agentic Plan Caching (\cite{plancache}) — semantic-
% signature cache at the plan layer; 50% cost / 27% latency win;
% complementary to prefix caching.
%
% \subsection{Prefix caching mechanics}
% Anthropic prompt-cache docs (\cite{anthropic-cache}); PagedAttention
% (\cite{pagedattention}); Mooncake (\cite{mooncake}); SGLang/vLLM
% RadixAttention. The baseline that plan caching stacks atop.

\tothink{Expand each subsection to $\sim$0.5 pages with proper citations.}
