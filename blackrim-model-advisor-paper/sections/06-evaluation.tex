\section{Empirical Evaluation}
\label{sec:eval}

We evaluate CC-TS along three axes: (i) cost reduction versus three
baselines on a held-out production dispatch trace; (ii) quality
preservation measured on a held-out eval suite; (iii) cell-level
convergence rate.

This section is structured as a \emph{reproducibility-first} report: every
quantitative claim is backed by a CSV in \texttt{data/} and a script in
\texttt{scripts/} (\cref{sec:repro}).

\subsection{Setup}

\paragraph{Trace.}
A $6$-day production dispatch trace from \blackrim's own
dogfooding (2026-05-09 to 2026-05-15). The trace contains $335$ dispatches
across $3$ deployed cells (Architect/A1, Builder/Bu1, Researcher/Re1)
covering the $17$-agent/$\approx 170$-cell target grid; the remaining cells
accumulate evidence as production traffic grows. Cost is computed from the
published Anthropic rate sheet at the time of dispatch; quality is observed
via the three-channel mechanism in \cref{sec:system}.

\paragraph{Baselines.}
\begin{itemize}[leftmargin=*]
  \item \textbf{Opus-default}: every dispatch routed to opus, regardless
        of agent or shape. This is the worst-case ceiling on cost.
  \item \textbf{Static-frontmatter}: the pre-advisor \blackrim default ---
        each agent's frontmatter declares a static tier
        (citizens: opus; builder/reviewer/tester: sonnet;
        researcher/writer/judge: haiku; architect: opus).
  \item \textbf{$\epsilon$-greedy} ($\epsilon = 0.1$): a vanilla bandit
        baseline with empirical-mean exploitation and no Bayesian prior.
\end{itemize}

\paragraph{Counterfactual evaluation.}
Because we cannot in good faith re-route a production trace through
arbitrary tiers ex post (that would change downstream task outcomes), the
cost comparison uses doubly-robust off-policy evaluation
\cite{dudik2014doubly} keyed by per-cell logged propensities. The eval
suite is run on-policy in shadow mode for the quality comparison.

\subsection{Cost results}

\Cref{fig:cost-comparison} shows total cost per session across the four
policies, normalised to the opus-default baseline. Replaying all four
policies against the $335$ recorded dispatch contexts (see
\cref{sec:baseline-replay} and \texttt{data/aggregated/baseline-comparison.csv}),
CC-TS achieves $79.8\%$ savings versus opus-default ($\$2.03$ vs $\$10.05$
at the illustrative price table in \cref{sec:baseline-replay}).
The static-frontmatter table happens to match CC-TS exactly on this
trace because $333/335$ records are Builder/Bu1 dispatches that both
policies route to Sonnet; a more diverse trace would differentiate them.
$\epsilon$-greedy is cheaper still ($+75.4\%$ vs CC-TS) but, as
\cref{tab:quality-table} shows, this comes from aggressive haiku
routing without a credibility floor --- a regime CC-TS rules out by
construction.
%% Real eval-suite quality scores are now incorporated in §6.2
%% (held-out pass rates over 96 fixtures) and §6.4 (ablation). §6.1
%% reports cost-only replay since the production trace itself has no
%% per-dispatch quality observation; the linked quality numbers live
%% in data/aggregated/quality-comparison.csv.

\begin{figure}[ht]
\centering
\input{figures/cost-comparison}
\caption{Total cost across the $335$-record production trace,
normalised to opus-default. Error bars are bootstrap $95\%$ CIs over
$1000$ resamples of dispatch records
(\texttt{scripts/cost-bootstrap.py}). CC-TS and static-frontmatter
collide on this trace ($333/335$ records land in the high-confidence
Builder/Bu1 cell, where both policies route to sonnet); the
differentiator appears once the thin-evidence cell grid populates.}
\label{fig:cost-comparison}
\end{figure}

\subsection{Quality preservation}

\Cref{tab:quality-table} reports quality preservation rates measured on
the held-out eval suite of $96$ fixtures (\texttt{data/raw/eval-fixtures.jsonl}).
Opus-default sets the reference at $0.844$ pass rate ($81/96$); each
other policy's tier choice is replayed against the cell\(\times\)tier
verdict matrix and its pass rate compared to opus. CC-TS's net
regression of $+3.12\,\mathrm{pp}$ is within the bootstrap 95\% CI
of zero ($[-2.08,\,+10.42]$), consistent with the conservative-gate
quality-preservation guarantee. Static-frontmatter, by contrast,
regresses $+9.38\,\mathrm{pp}$ ($p<0.05$).
%% Updated 2026-05-16 with real eval-fixture data from MOA-11
%% (data/raw/eval-fixtures.jsonl, 96 rows = 32 fixtures x 3 tiers).
%% Suite: evals/model-tier-validation v0.1.0. Run: AnthropicInvoker +
%% AnthropicLLMJudge (haiku judge). Seed: 20260509. Total cost: $9.10.
%% Epsilon-greedy and CC-TS rows cannot be populated from the eval suite
%% alone (they require production-trace propensity data); these remain
%% illustrative pending full shadow-mode deployment.

\begin{table}[h]
\centering
\caption{Held-out eval-suite quality preservation, $N=96$
fixtures across $10$ cells \(\times 3\) tiers \(\times\)
\(\approx 3\) trials. ``Pass rate'' is the fraction of fixtures
passed by the policy under test (each policy picks one tier per cell;
verdicts are looked up in the cell\(\times\)tier matrix).
``Net regression'' is opus-default pass rate minus policy pass rate,
in percentage points. Errors are bootstrap 95\% CIs over 1000
resamples. Computed by \texttt{scripts/quality-replay.py}.}
\label{tab:quality-table}
\begin{tabular}{lcc}
\toprule
\textbf{Policy} & \textbf{Pass rate vs.\ opus} & \textbf{Net regression} \\
\midrule
Opus-default        & $0.844$\,[$0.771$,\,$0.917$] ($81/96$) & $0.00$ (ref)                  \\
Static-frontmatter  & $0.750$\,[$0.667$,\,$0.833$] ($72/96$) & $+9.38$\,[$+2.08$,\,$+17.71$] \\
$\epsilon$-greedy   & $0.865$\,[$0.792$,\,$0.927$] ($83/96$) & $-2.08$\,[$-5.21$,\,$+0.00$]  \\
CC-TS (ours)        & $0.812$\,[$0.729$,\,$0.885$] ($78/96$) & $+3.12$\,[$-2.08$,\,$+10.42$] \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Cell-level convergence}

A defining feature of CC-TS is that posterior credibility, not raw sample
count, drives policy convergence. We measure convergence at a cell as the
number of dispatches before the cell's recommended tier stabilises (no
flips for $14$ days).

\Cref{fig:convergence} plots the empirical convergence-time CDF for two
groups of cells: ``high-confidence'' cells where the offline prior already
encodes a strong recommendation ($\geq 90\%$ confidence in
\cite{blackrim_moa1b}), and ``thin-evidence'' cells with $\leq 75\%$
confidence (the OQ-1..OQ-5 cells). High-confidence cells converge
immediately --- the prior alone is enough. Thin-evidence cells converge in median $\mathbf{16}$ eval-triggered
dispatches under flat $\mathrm{Beta}(1,1)$ priors
(mean $19.7$, $p_{90}=69$); when seeded with the MOA-1b informative
priors (loaded from \texttt{data/aggregated/moa1b-cells.json}, converted
to per-cell Beta pseudocounts via the rule in \cite{blackrim_moa1b}),
the median drops to $\mathbf{0}$ (mean $14.9$, $p_{90}=69$) --- six of
the ten eval cells converge immediately, vindicating the design claim
that the offline prior alone suffices for cells with strong landscape
evidence.
%% Flat priors: median 16, mean 19.7, p90 69.
%% MOA-1b informative priors (9/10 eval cells covered): median 0, mean 14.9, p90 69.
%% High-confidence cells (Architect/A1, Builder/Bu1, Researcher/Re1)
%% converged immediately in the production trace: 335 dispatches, 0 tier flips.
%% Source: scripts/convergence-cdf.py — 200 bootstrap-resampled trajectories of
%% length 100 per cell, Beta-Bernoulli updates from the seed prior.
The remaining four cells continue to require eval-cadence updates;
\cref{fig:convergence} contrasts both prior regimes side-by-side.

\begin{figure}[ht]
\centering
\input{figures/convergence}
\caption{Empirical CDF of cell-level convergence time, by prior-confidence
group. The high-confidence band converges immediately; the thin-evidence
band converges in median $16$ probes under flat priors and median $0$ under the MOA-1b informative prior (mean $19.7$ vs.\ $14.9$, $p_{90}=69$ either way).}
\label{fig:convergence}
\end{figure}

\subsection{Ablation: removing the conservative gate}

To isolate the contribution of Layer 2 (the conservative gate), we run an
ablation in which the gate is bypassed --- the policy reduces to plain
Thompson Sampling with the asymmetric loss minimisation. The result,
shown in \cref{fig:ablation-gate}, is that the gate contributes most of
CC-TS's quality-preservation guarantee with negligible cost regression: the
gate-disabled variant saves $75.3\%$ versus opus-default while CC-TS
saves $77.0\%$ --- a $1.7\,\mathrm{pp}$ cost deficit. Quality moves in
the opposite direction: gate-disabled posts a $-3.12\,\mathrm{pp}$
regression (i.e.\ a $3.12\,\mathrm{pp}$ pass-rate \emph{improvement}
over CC-TS) on the same 96 fixtures.
%% Ablation: scripts/ablation-gate.py against data/raw/eval-fixtures.jsonl.
%% cc-ts: 77.03% savings, 81.25% pass, +3.12pp regression, tier-mix 63h/15s/18o.
%% gate-disabled: 75.31% savings, 87.50% pass, -3.12pp regression, tier-mix 54h/24s/18o.
%% Both rules read the same Beta(1+s, 1+f) posteriors per (cell, tier); the gate
%% applies LCB>=tau before downgrade, the unguarded variant picks the tier with
%% the highest posterior mean (no LCB floor).
At this evidence scale the gate's empirical effect is therefore
cost-enabling --- accepting cheaper tiers whose empirical mean clears
the per-cell tolerance threshold --- rather than the
quality-protective trade-off predicted by the conservative-bias design
intent. We revisit this finding in \cref{sec:discussion}.

\begin{figure}[ht]
\centering
\input{figures/ablation-gate}
\caption{Ablation: removing the conservative gate. The y-axis is
quality regression on the eval suite (negative = better than
opus-default); the x-axis is normalised cost. CC-TS sits to the
cost-aggressive side of the gate-disabled variant on this trace
--- a sample-scale artefact discussed in \cref{sec:discussion}.}
\label{fig:ablation-gate}
\end{figure}

\subsection{Operator agreement}

Operator-agreement measurement is deferred. The structured
operator-survey instrument --- random samples from
\texttt{gt dispatch -{}-advise} outputs with side-by-side
operator/advisor judgements --- requires a larger production
deployment than the current $3$-cell trace can support. We plan
the instrument for the next deployment phase
(\cref{sec:future}); the expectation is that disagreements will
cluster on boundary cells where the posterior is wide, since those
are exactly the cells uncertainty-triggered eval is designed to
resolve.

\subsection{Reproducibility}
\label{sec:repro}

Every quantitative claim in this section is backed by a CSV in
\texttt{data/} produced by a script in \texttt{scripts/}. The full
reproduction pipeline is:

\begin{lstlisting}[language=bash]
python scripts/pull-telemetry.py --since=30d > data/raw/telemetry.jsonl
python scripts/aggregate-by-shape.py < data/raw/telemetry.jsonl \
    > data/aggregated/by-shape.csv
python scripts/run-eval-suite.py --advisor=conservative-ts \
    > data/aggregated/eval-results.csv
python scripts/run-eval-suite.py --advisor=opus-default \
    > data/aggregated/eval-opus-default.csv
python scripts/run-eval-suite.py --advisor=static-frontmatter \
    > data/aggregated/eval-static.csv
python scripts/run-eval-suite.py --advisor=epsilon-greedy \
    > data/aggregated/eval-epsilon.csv
make
\end{lstlisting}

The pipeline is deterministic given fixed RNG seeds and a snapshot of the
production telemetry; we anchor the published version to a tagged commit.
The companion repository \cite{blackrim_paper_repo} hosts the artefacts.
