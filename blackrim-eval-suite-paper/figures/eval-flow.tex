% Placeholder eval-flow figure — replaced by the real TikZ diagram when filled in.
\fbox{\parbox{0.9\linewidth}{\centering\itshape
  [eval-flow diagram placeholder — task input → scorer (heuristic /
  embedding / LLM-classifier / LLM-rubric / trajectory) → per-cell
  calibration + agreement + bias-bound axes → suite verdict]
}}
