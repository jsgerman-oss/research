# RC-12 — §0 abstract + Appendix + final polish

## Goal
Final-mile work: ensure §0 abstract reflects every measured number,
fill the Appendix, kill `\tothink` markers, produce a clean `main.pdf`,
build the Overleaf-ready zip.

## Context — read first
- Paper dir: `/Users/jayse/research/blackrim-routing-caching-paper/`
- Appendix placeholder: `sections/A-appendix.tex`
- All prior phases RC-01..11 must be complete. Run
  `grep -rn '\\tothink' sections/` first — these MUST go to zero.

## Tasks
- [ ] **Abstract.** Rewrite `sections/00-abstract.tex`. Target
  200-250 words. Required content:
  1. *Setting.* Static-prefix multi-agent coding systems with a
     long-running orchestrator and per-task subagent dispatches.
  2. *Measurement.* In one production system (Blackrim, 7-day
     window, claude-opus-4-7), main-thread orchestration accounts
     for 99.4% of session cost; subagent dispatch is already
     tuned to 0.6%.
  3. *Method.* Two complementary primitives — per-turn semantic-
     similarity routing and agentic plan caching — derived from
     the cost-decomposition.
  4. *Result.* Measured routing saves X% on the held-out test
     set with Y% opus-recall preserved; plan caching adds Z%
     additional savings at <5% false-hit rate; composed system
     saves W% vs prefix-cache-only baseline.
  5. *Artifacts.* Scripts + CSVs + this paper repo are public;
     telemetry from the production instance is available on
     request.
- [ ] **Appendix §A.** Rewrite `sections/A-appendix.tex`.
  Subsections:
  1. **§A.1 — Reproducibility cookbook.** Clean-clone-to-PDF
     shell transcript. Should be 8-12 lines.
  2. **§A.2 — Full cost-split table.** Render
     `data/aggregated/cost-split.csv` as `longtable` (per-bucket
     dollars + share + n_calls + percent breakdown).
  3. **§A.3 — Routing test set.** Render the 10 held-out turns +
     gold labels + per-router predictions. The reader can audit
     the labels.
  4. **§A.4 — Plan-cache signature examples.** Show 5-10 dispatch
     signatures from the replay set (illustrate the abstraction
     level chosen in §6).
  5. **§A.5 — Pricing assumptions.** Embed the
     `internal/pricing/sheets/anthropic-public.toml` rates used
     by `scripts/aggregate-cost-split.py`. Future reprice without
     re-running.
- [ ] **`\tothink` sweep.** `grep -rn '\\tothink' sections/`. Each
  remaining marker: complete the work OR delete the sentence +
  promote to §9 future work. Zero remaining at end.
- [ ] **Final build + check.** `make clean && make 2>&1 | tee
  /tmp/rc-build.log`. Verify:
  - `main.pdf` exists, page count 18-30.
  - Zero `Reference X undefined`.
  - Zero `Citation X undefined`.
  - Four figures present (cost-split, cache-trajectory, routing,
    composed-cost).
  - Two algorithm blocks present (one each in §5 and §6).
  - All four §A subsections render.
- [ ] **Cosmetics.** `make wordcount` and check section
  distribution. Make a pass for terminology consistency: every
  reference to the two primitives uses the same names
  ("per-turn semantic-similarity routing", "agentic plan caching")
  consistently across §0/§1/§5/§6/§7/§10. The notation macros
  from main.tex should be the only way math objects are written.
- [ ] **README.** Update `README.md` "Status" section to reflect
  draft-complete.
- [ ] **Final commit.** `release(rc): draft v1 complete — abstract +
  appendix + final polish; pdf builds clean`.
- [ ] **Bundle.** `Skill(paper-bundle)` or
  `cd .. && zip -r blackrim-routing-caching-paper.zip
   blackrim-routing-caching-paper -x '**/__pycache__/*'
   '**/build/*' '**/.embedding-cache/*'`.

## Acceptance criteria
- `main.pdf` builds clean (no warnings).
- Zero `\tothink` markers anywhere in `sections/`.
- Abstract names the four concrete measured numbers (cost-split %,
  routing %, plan-cache %, composed %).
- §A Appendix renders all five subsections.
- `blackrim-routing-caching-paper.zip` exists at repo root.

## What this phase does NOT do
- arxiv submission packaging.
- LICENSE file (TODO at submission time).
- Author-affiliation final formatting.
- The companion-paper cross-cite resolution (the trim paper must
  also be `release(trim)` before the cross-cite hyperlinks). If
  the trim paper isn't ready, leave the cross-cite as text "see
  the companion instruction-trim paper" without `\cite{trim}`.
