# RC-08 — Bibliography fill

## Goal
Populate `bib/refs.bib` with the citations referenced throughout the
paper, and verify `make` builds without `Citation X undefined` warnings.

## Context
- Paper dir: `/Users/jayse/research/blackrim-routing-caching-paper/`
- biblatex backend = biber, style = numeric-comp.
- Required external citations (from README + sections):
  - **FrugalGPT** — Chen et al. 2023 (arXiv:2305.05176)
  - **RouteLLM** — Ong et al. 2024 (arXiv:2406.18665)
  - **Tryage** — 2023 (arXiv:2308.11601)
  - **Confidence-based routing** — Aggarwal et al. 2024
    (arXiv:2410.13284) — verify the v3
  - **Agentic Plan Caching** — NeurIPS 2025 paper (find canonical
    title + authors via the deep-dive doc cite-chain; do NOT
    fabricate)
  - **Anthropic prompt caching** — Anthropic docs misc cite
  - **Anthropic cache-control marker docs** — same
  - **Lost in the Middle** — Liu et al. (arXiv:2307.03172) if §2
    surveys context degradation
- Internal docs (cite as `@misc` with commit SHA):
  - `tier-down-main-thread-landscape.md` (commit ecfdcba)
  - `cache-control-deep-dive.md` (commit 660054d)
  - `multi-agent-optimization-landscape.md` (commit aa9ddba)
  - `model-cost-quality-landscape.md` (commit 59d333d)
  - `cache-control-markers.md` if referenced

## Tasks
- [ ] Enumerate every cite key:
  `grep -ohE '\\\\cite[a-z]*\\{[^}]+\\}' sections/*.tex | sort -u`.
- [ ] Populate `bib/refs.bib`. For each external arxiv cite, use:
  ```
  @article{ong2024routellm,
    author = {Ong, Isaac and ...},
    title = {RouteLLM: Learning to Route LLMs with Preference Data},
    journal = {arXiv preprint},
    eprint = {2406.18665},
    archivePrefix = {arXiv},
    primaryClass = {cs.LG},
    year = {2024},
  }
  ```
  Verify each {author list, title, year} against the actual arxiv
  abstract page before committing. If you can't verify, do NOT
  fabricate — leave a `% TODO(cite): verify <key>` and drop the
  prose cite.
- [ ] For internal Blackrim docs:
  ```
  @misc{tier-down-landscape,
    author = {German, Jay},
    title = {Tier-Down Main-Thread Routing: A Landscape Survey},
    howpublished = {Blackrim repo, commit \\texttt{ecfdcba}},
    year = {2026},
  }
  ```
- [ ] Re-grep after population:
  `grep -ohE '\\\\cite[a-z]*\\{[^}]+\\}' sections/*.tex | sort -u`
  vs `grep -oE '^@[a-z]+\\{[^,]+' bib/refs.bib | sort -u`. The first
  set must be a subset of the second.
- [ ] Build: `make clean && make`. Zero `Citation X undefined`.
- [ ] Page-check the bibliography. Open `main.pdf` — references
  should render numbered, links work, no missing fields ("?" in
  the bibliography output).
- [ ] Commit: `bib(rc): populate refs.bib — routing literature
  (FrugalGPT, RouteLLM, Tryage, confidence routing) + plan-caching
  + Anthropic prompt-caching + internal landscape docs`.

## Acceptance criteria
- Every cite key in `sections/*.tex` resolves.
- `make` builds with zero citation warnings.
- All external citations verified against arxiv abstract pages
  (or canonical source). No fabrications.

## Failure mode
- A cite key in prose without a known reference: leave a
  `% TODO(cite): need reference for <claim>` comment and remove
  the cite from prose. Don't bib-stub-fabricate.
