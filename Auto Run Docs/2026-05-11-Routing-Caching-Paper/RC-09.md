# RC-09 — Flesh §1 Introduction + §2 Related Work

## Goal
Take §1 and §2 from placeholder to publication prose. By the end of
this phase, the paper reads coherently from §0 abstract through §2
related work. Word-count targets:
- §1 Introduction: 700-1000 words
- §2 Related Work: 1000-1400 words (this paper surveys two literatures
  — routing AND caching — so §2 is heavier than the trim paper's)

## Context — read first
- Paper dir: `/Users/jayse/research/blackrim-routing-caching-paper/`
- Source substrate (do not re-research — synthesise):
  - `/Users/jayse/Code/blackrim/docs/research/tier-down-main-thread-landscape.md`
    (commit ecfdcba) — 7500 words on routing literature
  - `/Users/jayse/Code/blackrim/docs/research/cache-control-deep-dive.md`
    (commit 660054d) — caching literature + Blackrim's empirical
    measurement
  - `/Users/jayse/Code/blackrim/docs/research/multi-agent-optimization-landscape.md`
    (commit aa9ddba) — the 9000-word ranked optimisation survey
  - `/Users/jayse/Code/blackrim/docs/research/model-cost-quality-landscape.md`
    (commit 59d333d) — empirical cost-quality matrix
  - The current §3, §4, §5, §6, §7 in this paper
- Current placeholders:
  - `sections/01-introduction.tex`
  - `sections/02-related-work.tex`
- RC-08 (bibliography) should be complete; if any cite you need
  isn't in refs.bib, add it.

## Tasks
- [ ] **§1 Introduction.** Rewrite `sections/01-introduction.tex`:
  1. *Hook* (1 paragraph). Open with the 99.4% measurement (cite
     §7 Table 1 and fig1): for a static-prefix multi-agent coding
     system, dispatch-side subagent routing has already been
     squeezed (0.6% share); the orchestrator itself is the
     remaining cost lever.
  2. *Problem statement* (1 paragraph). Two complementary primitives
     are candidates: (a) per-turn model routing for the orchestrator,
     (b) plan-level caching across the 5-min prefix-cache TTL window.
     Neither has a public methodology grounded in production
     multi-agent telemetry.
  3. *Approach* (1 paragraph). Semantic-similarity routing + agentic
     plan caching. Cite §5 and §6 (the algorithm sections).
  4. *Contributions* (4 bullets):
     (i) cost-decomposition measurement showing 99.4% main-thread
         dominance,
     (ii) semantic-similarity router with measured precision/recall
         on a 10-turn held-out test set,
     (iii) plan-cache replay measurement on N historical dispatches,
     (iv) composed-cost analysis showing the two primitives
         compound.
  5. *Roadmap* (1 short paragraph).
- [ ] **§2 Related Work.** Rewrite `sections/02-related-work.tex`.
  Organise as TWO sub-strands, each 3-4 paragraphs:
  - **§2.1 — Model routing.** Survey FrugalGPT (sequence-of-calls
    cascade), RouteLLM (preference-learned router), Tryage
    (capability-aware router), confidence-based routing (Aggarwal
    et al.). For each: one paragraph describing the approach,
    one sentence on its applicability to per-turn multi-agent
    orchestration. Then a paragraph on what's missing in the
    literature for our setting (no public per-turn router for
    long-running orchestration with structured tool use).
  - **§2.2 — Caching for LLM systems.** Survey prefix caching
    (vLLM/SGLang/Anthropic cache_control), plan-level / semantic
    caching (the NeurIPS 2025 agentic plan caching paper as the
    direct antecedent), and the gap our work fills (the
    composed-cost measurement combining prefix + plan + routing
    in one system).
- [ ] Update `sections/00-abstract.tex` to reflect the fleshed
  intro. Abstract structure: motivation (99.4% main-thread cost),
  method (two primitives + the cost model relating them), result
  (the measured numbers from RC-04 routing + RC-06 plan-cache +
  composed), reproducibility. Target 200-250 words (this paper's
  abstract is larger than trim's because it covers two
  contributions).
- [ ] Build: `make clean && make`. Resolve missing cites.
- [ ] Run `make wordcount`; record in commit body.
- [ ] Commit: `prose(rc): flesh §1 intro, §2 related work
  (routing + caching strands); update §0 abstract`.

## Acceptance criteria
- §1 and §2 meet word-count targets ±20%.
- Every `\cite{}` resolves.
- No `\tothink{}` in §1 or §2.
- §0 abstract names the concrete measured numbers.

## Style guidance
- §1 hook must lead with the empirical measurement, not a
  literature claim. The 99.4% number is the paper's strongest
  motivating evidence — anchor on it.
- §2 should be honest about the lineage: agentic plan caching
  is from NeurIPS 2025; this paper's contribution is empirical,
  not algorithmic-novel, on that side. Don't oversell.
- No back-references to future sections inside §1 ("as shown in
  §7…"); §1 should stand alone.
