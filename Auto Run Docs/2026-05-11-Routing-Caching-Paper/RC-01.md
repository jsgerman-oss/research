# RC-01 — Cache-stats telemetry pull + aggregation

## Goal
Replace the illustrative
`/Users/jayse/research/blackrim-routing-caching-paper/data/raw/session-evidence.json`
with **real** records pulled from Blackrim's
`.beads/telemetry/invocations.jsonl`, then regenerate
`data/aggregated/cache-stats.csv` and reconcile against the §7 "Cache
calibration baseline" numbers (read_to_creation_ratio 4.56,
cache_hit_rate_estimate 0.793, savings_vs_no_caching 0.693).

## Context — read before touching anything
- Paper dir: `/Users/jayse/research/blackrim-routing-caching-paper/`
- Blackrim repo: `/Users/jayse/Code/blackrim`
- Telemetry file: `/Users/jayse/Code/blackrim/.beads/telemetry/invocations.jsonl`
  — verify exists before running anything.
- Pull script: `scripts/pull-telemetry.py` — reads the JSONL,
  filters by --since date, emits one JSON line per invocation with
  `cache_creation_input_tokens` + `cache_read_input_tokens` fields.
- Aggregate script: `scripts/aggregate-cache-stats.py` — sums creation
  + read tokens, computes ratio + cache-enabled-spawn rate.
- §7 has hardcoded numbers (`sections/07-evaluation.tex` lines 41-56):
  read-to-creation 4.56×, cache hit rate 79.3%, savings 69.3%. After
  refresh, these must match the CSV or be updated to match.
- The current `data/raw/session-evidence.json` is NOT a session-telemetry
  pull output — it's a mixed-kind evidence file with `kind`-tagged
  records ("session-evidence", "cache-baseline", "phase-A-recommendation",
  etc.). This phase REPLACES the cache-stats sub-record with measured
  data; it doesn't touch the other kinds.

## Tasks
- [ ] Verify the telemetry file exists:
  `test -f /Users/jayse/Code/blackrim/.beads/telemetry/invocations.jsonl
  && wc -l /Users/jayse/Code/blackrim/.beads/telemetry/invocations.jsonl`.
  Expect thousands of lines. If missing, STOP and report — do not
  fabricate.
- [ ] Run the pull:
  `python scripts/pull-telemetry.py --since 2026-05-01
   --repo /Users/jayse/Code/blackrim
   > data/raw/session-telemetry.json`. This file is new; it's
  separate from `session-evidence.json` (which stays as the hand-
  curated kind-tagged evidence file). Verify ≥100 records:
  `wc -l data/raw/session-telemetry.json`.
- [ ] Run the aggregate:
  `python scripts/aggregate-cache-stats.py
   < data/raw/session-telemetry.json
   > data/aggregated/cache-stats.csv`. Verify it has the 6-row
  metric/value format (cache_creation_total_tokens,
  cache_read_total_tokens, read_to_creation_ratio,
  cache_enabled_spawn_rate, sample_count, cache_enabled_count).
- [ ] Cross-check against §7 inline bullets (lines 49-52):
  - read_to_creation_ratio: was 4.56; CSV says X.XX
  - cache_enabled_spawn_rate: was 0.793; CSV says X.XXX
  - savings_vs_no_caching 0.693: this is a DERIVED number — verify
    the derivation. The deep-dive doc
    `/Users/jayse/Code/blackrim/docs/research/cache-control-deep-dive.md`
    has the math. Likely: savings = read_total / (read_total +
    creation_total · (1/0.10)) or similar. Trace the derivation,
    document it as a script if not already present.
- [ ] If any §7 number drifts from CSV by >5%, update §7 with the
  new value AND note the drift in the commit message body.
- [ ] Add a `make data` target (or verify exists) that runs the
  pull + aggregate end-to-end and writes both CSVs. Verify
  idempotence: `make data && make data` produces no diff.
- [ ] Commit: `data(rc): cache-stats CSV from blackrim telemetry
  (2026-05-01 → now); §7 cache-baseline reconciled`.

## Acceptance criteria
- `data/raw/session-telemetry.json` has ≥100 real invocation
  records (one per spawn).
- `data/aggregated/cache-stats.csv` exists with the 6-metric rows.
- §7 cache-baseline bullets either match CSV unchanged or are
  updated.
- `make data` is idempotent.

## Out of scope
- Cost-split telemetry (RC-02 handles the 99.4% / 0.6% measurement).
- Routing eval (RC-03/04).
- Plan-cache eval (RC-05/06).
