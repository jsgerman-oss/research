% Placeholder retriever-flow figure — replaced by the real TikZ diagram
% when figures/retriever-flow.tex is filled in. Keeps compile green.
\fbox{\parbox{0.9\linewidth}{\centering\itshape
  [retriever-flow diagram placeholder — query class → bandit arm →
  BM25 + SPLADE + dense fan-out → RRF fusion → decay/MMR →
  optional reranker → paper-stream telemetry]
}}
