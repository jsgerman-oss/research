% figures/paired-tokens-distribution.tex — per-task Δ_tokens distribution.
% Backs §6.7 (OQ-AST-5). Box-and-whisker of weighted per-task token deltas
% (ON minus OFF; negative = savings), with median + 95% BCa CI annotated.
%
% Data source (post-eval): data/scratch/paper-v6.7/figures-data/paired-tokens-distribution.json
% Schema: { "delta_tokens": [...], "median": float, "bca_lo": float, "bca_hi": float, "n": int }
%
% Data-dir override: redefine \paperv67datadir before \input{figures/...} if needed.
% Placeholder phase: compiles to a "Data pending" notice until JSON is populated.
%
% Generated by blackrim-0cq7 (F6).

% Macro for data directory — override in main.tex or per-document preamble if needed.
\providecommand{\paperv67datadir}{data/scratch/paper-v6.7/figures-data}

\begin{tikzpicture}
\begin{axis}[
    width=0.85\textwidth,
    height=7cm,
    xlabel={Per-task $\Delta_\text{tokens}$ (ON$-$OFF; negative $=$ savings)},
    ylabel={Density},
    xmin=-200, xmax=200,
    ymin=0, ymax=0.003,
    grid=major,
    grid style={dashed,gray!20},
    xticklabel style={font=\small},
    yticklabel style={font=\small},
]

% --- Placeholder bar ----------------------------------------------------------
% When real data land, replace this block with pgfplots boxplot or histogram
% coordinates loaded from \paperv67datadir/paired-tokens-distribution.json.
\addplot+[
    ybar interval,
    fill=gray!20,
    draw=gray!60,
    bar width=100pt,
] coordinates { (-100, 0.001) (0, 0.001) (100, 0.001) };

\node[anchor=center, font=\small\itshape, text=gray!70] at (axis cs:0,0.0015)
    {Data pending --- run \texttt{scripts/fill-paired-trace-macros.py} after eval};

% --- Annotation box for headline statistics (macro-driven) -------------------
% \headlineDeltaTokens and \headlineDeltaTokensCI are defined in §6.7 preamble.
% They render as "TBD" in the pending phase; replace with numbers post-eval.
\node[
    draw=blue!40,
    fill=blue!5,
    rounded corners=2pt,
    font=\scriptsize,
    align=left,
    anchor=north east,
] at (axis cs:195,0.0028) {%
    Median $\Delta$: \headlineDeltaTokens\\
    95\% BCa CI: \headlineDeltaTokensCI
};

\end{axis}
\end{tikzpicture}
