% figures/paired-success-distribution.tex — per-task Δ_success bar chart.
% Backs §6.7 (OQ-AST-5). Three bars: Δ_success ∈ {-1, 0, +1}, i.e.
% count of tasks where ON degraded / preserved / improved success.
%
% Data source (post-eval): data/scratch/paper-v6.7/figures-data/paired-success-distribution.json
% Schema: { "counts": {"-1": int, "0": int, "1": int},
%            "prop_nonneg": float, "wilson_lo": float, "wilson_hi": float, "n": int }
%
% Data-dir override: redefine \paperv67datadir before \input{figures/...} if needed.
% Placeholder phase: compiles to a "Data pending" notice until JSON is populated.
%
% Generated by blackrim-0cq7 (F6).

% \paperv67datadir defined in paired-tokens-distribution.tex; \providecommand is safe.
\providecommand{\paperv67datadir}{data/scratch/paper-v6.7/figures-data}

\begin{tikzpicture}
\begin{axis}[
    width=0.75\textwidth,
    height=6cm,
    ybar,
    bar width=28pt,
    enlarge x limits=0.3,
    ymin=0, ymax=55,
    ylabel={Task count},
    xlabel={$\Delta_\text{success}$ (ON $-$ OFF)},
    symbolic x coords={{$-1$},{$0$},{$+1$}},
    xtick=data,
    xticklabel style={font=\small},
    nodes near coords,
    nodes near coords align={vertical},
    nodes near coords style={font=\scriptsize},
    grid=major,
    grid style={dashed,gray!20},
    legend style={at={(0.97,0.97)}, anchor=north east, font=\small},
]

% --- Placeholder bars ---------------------------------------------------------
% When real data land, replace the coordinates below with values from
% \paperv67datadir/paired-success-distribution.json (counts["-1"], counts["0"],
% counts["1"]).  A fill-paired-trace-macros.py run rewrites these automatically.
\addplot+[fill=red!30, draw=red!70!black] coordinates {
    ({$-1$}, 0)
};
\addplot+[fill=gray!30, draw=gray!70!black] coordinates {
    ({$0$}, 0)
};
\addplot+[fill=blue!30, draw=blue!70!black] coordinates {
    ({$+1$}, 0)
};

\node[anchor=center, font=\small\itshape, text=gray!70] at (axis cs:{$0$},25)
    {Data pending};

\legend{Degraded, Preserved, Improved}

% --- Wilson CI annotation (macro-driven) -------------------------------------
\node[
    draw=blue!40,
    fill=blue!5,
    rounded corners=2pt,
    font=\scriptsize,
    align=left,
    anchor=north west,
] at (axis cs:{$-1$},50) {%
    $\Pr(\Delta_\text{succ} \geq 0)$: \successProportionAtLeastZero\\
    95\% Wilson CI: \successProportionCI
};

\end{axis}
\end{tikzpicture}
