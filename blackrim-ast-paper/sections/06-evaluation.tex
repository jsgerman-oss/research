\section{Empirical Evaluation}
\label{sec:eval}

We evaluate the AST surface along six axes: (i) cross-language
compression ratios on a realistic benchmark fixture; (ii) outline
token-budget conformance on a wider corpus; (iii) outline emission
latency; (iv) outline-discipline adoption trajectory; (v) symbolic
surgery false-negative rate; (vi) end-to-end token reduction on real
agent traces.

\paragraph{What is measured now vs.\ pending.}
Three of the six claims are currently measured:
\begin{itemize}[leftmargin=*,nosep]
  \item \textbf{Compression ratios} (\cref{sec:eval:compression}) are
        measured on a 40-file polyglot corpus (10 files per language)
        drawn from open-source repositories (\cref{sec:eval:setup}) and
        backed by a runnable CSV pipeline.
  \item \textbf{Token-budget conformance} (\cref{sec:eval:budget}) and
        \textbf{outline emission latency} (\cref{sec:eval:latency}) are
        measured on Phase~1a (Go-only, three repositories, 600 files
        sampled), with Python/JS/TS deferred to Phase~1b.
\end{itemize}
The remaining three axes correspond to pre-registered open questions
OQ-AST-3 through OQ-AST-5 (\cref{app:open-questions}) whose datasets
are still accumulating:
OQ-AST-3 (adoption trajectory) needs at least 14~days of federated
outline-events telemetry;
OQ-AST-4 (compile-gate false-negative rate) needs a curated 200-diff
refactor corpus;
OQ-AST-5 (end-to-end token reduction) needs paired Explore-tier
traces with and without outline discipline enabled.
These are deferred to follow-up work and are not considered failures
of the current system --- they are the pre-registered programme that
will promote, scope, or demote subsystems as data arrive.

This section is structured as a \emph{reproducibility-first} report:
every quantitative claim is backed by a CSV in \texttt{data/} and a
script in \texttt{scripts/} (\cref{sec:repro}).
\Cref{tab:eval-status} summarises the measured vs.\ pending partition.

\begin{table}[ht]
\centering
\caption{Status of empirical claims. Compression ratios are measured
  on a 40-file polyglot corpus ($n{=}10$ per language); token-budget
  conformance and latency are measured on Phase~1a (Go, three repos).
  The remaining three are pre-registered open questions whose datasets
  are still accumulating.}
\label{tab:eval-status}
\small
\begin{tabular}{lll}
\toprule
\textbf{Claim} & \textbf{Section} & \textbf{Status} \\
\midrule
Compression ratios by language        & \cref{sec:eval:compression}  & \textbf{Measured ($n{=}10$/lang)} \\
Token-budget conformance              & \cref{sec:eval:budget}       & \textbf{Measured (Phase 1a)} \\
Outline emission latency              & \cref{sec:eval:latency}      & \textbf{Measured (Phase 1a)} \\
Outline-discipline adoption           & \cref{sec:eval:adoption}     & Pending (OQ-AST-3) \\
Compile-gate false-negative rate      & \cref{sec:eval:gate}         & Pending (OQ-AST-4) \\
End-to-end token reduction on agents  & \cref{sec:eval:e2e}          & Pending (OQ-AST-5) \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Setup and corpus}
\label{sec:eval:setup}

\paragraph{Bench corpus (v2).}
The compression-ratio measurement uses a 40-file polyglot corpus
(10 files per language) in \texttt{scripts/corpus-files/\{go,py,js,tsx\}/}.
Files were drawn from four open-source repositories already cloned in
\texttt{data/scratch/corpora/} and from the Blackrim checkout, chosen
to represent varied shapes: API handler, data model, test harness, CLI
command, utility, and domain-specific computation.
LoC ranges: Go 76--1098 (median 225), Python 68--370 (median 118),
JavaScript 60--125 (median 81), TypeScript 58--248 (median 83).
The measurement uses a structural-outline approximation
(declaration-header lines retained, body lines dropped) implemented in
\texttt{pull-compression-ratios.py}; this is consistent with the
\texttt{gt outline} AST walk for the declaration surface the paper
focuses on.\footnote{Prior to v2 ($n{=}1$ per language, single in-tree
bench fixture), measured token savings were: Go 82.0\%, Python 84.1\%,
JavaScript 83.0\%, TypeScript 75.2\%
(\texttt{cmd/gt/testdata/bench/sample.\{go,py,js,tsx\}}). The v2 corpus
supersedes these for the purposes of §\ref{sec:eval:compression}.}

\paragraph{Corpus.} The broader corpus for OQ-AST-1 (token-budget
conformance), OQ-AST-2 (latency), and OQ-AST-5 (end-to-end) consists
of: (a) the Blackrim repository itself ($\sim$3\,175 source files
across Go, Python, JavaScript, and TypeScript, excluding worktrees,
testdata, and vendored directories); (b) a public-repo polyglot sample
pinned in \texttt{scripts/corpus.txt}: the Go standard library, the
CPython standard library subset, the React source tree, the TypeScript
compiler, and Kubernetes. Files with \texttt{outline:skip} or in
vendored directories are excluded. This broader corpus is reserved for
the pending OQs; it was not used for the compression-ratio measurement
in \cref{sec:eval:compression}.

\paragraph{Telemetry.}{\sloppy
Adoption-trajectory data (OQ-AST-3) is sourced
from the \emph{federated} outline-events sink at
\texttt{\$BLACKRIM\_HOME/.beads/telemetry/outline-events.jsonl}
(default \texttt{\$HOME/Code/blackrim/}), to which the PreToolUse
hook (\cref{sec:system:discipline}) writes a copy of every qualifying
event across the Blackrim factory \emph{and} every factory project the
user works in (those onboarded via \texttt{blackrim-project on}). The
hook also keeps a per-project copy at
\texttt{<project\_root>/.beads/telemetry/outline-events.jsonl}
for project-local dashboards, but the central sink is the
paper's load-bearing source. Schema fields:
\texttt{ts, tool, file, loc, outline\_called\_prior, verdict, agent,
transcript\_id, project\_root}. The
\texttt{project\_root} tag lets us group adoption by factory vs.\
consumer projects and detect whether the discipline ports cleanly
across the \texttt{blackrim-project on} boundary (relevant to
\cref{sec:discussion:limits}). End-to-end traces (OQ-AST-5) are
sourced from \texttt{.beads/telemetry/invocations.jsonl} in the
corresponding project root, joined to outline events by transcript ID.\par}

\subsection{Compression ratios by language}
\label{sec:eval:compression}

\Cref{fig:compression} reports the mean token-savings ratio
$1 - \ratio(\file)$ per language across the 10-file per-language corpus
(v2). All four languages clear the per-language floors enforced by the
in-tree benchmark
assertion\footnote{\texttt{cmd/gt/compress\_structure\_bench\_test.go}}:
Go~65.2\% (floor 50\%), Python~72.3\% (floor 20\%),
JavaScript~78.1\% (floor 50\%), TypeScript~75.7\% (floor 50\%).

\begin{figure}[ht]
\centering
\input{figures/compression-by-lang}
\caption{Mean token-savings ratio $1 - \ratio(\file)$ by language on
the 40-file polyglot corpus (v2; $n{=}10$ per language). Per-language
floors enforced by the benchmark suite are: Go 50\%, Python 20\%,
JavaScript 50\%, TypeScript 50\% \cite{blackrim_adr_0002}; measured
values clear the floors in all four languages. Go's savings (65.2\%)
are lower than in the prior $n{=}1$ fixture (82.0\%) because the v2
corpus includes large files (e.g.\ \texttt{eval/runner.go} at
1098~LoC) whose public-function signatures occupy more outline space
relative to body.}
\label{fig:compression}
\end{figure}

\paragraph{Why the per-language variance.}
Python's highest savings (72.3\%) reflect the language's indented
block structure: function and class bodies are large relative to their
headers, so dropping body lines yields a small outline. JavaScript
achieves the highest savings in v2 (78.1\%), driven by the React
source corpus which has many small single-component files where the
export declaration and function header are the bulk of the structural
surface. Go's savings are lower in v2 (65.2\%) than in the prior
$n{=}1$ measurement (82.0\%); the v2 corpus intentionally includes
large files with many exported functions whose full signatures are
preserved by the outline, reducing the body-drop ratio. TypeScript
(75.7\%) is stable across v1 and v2 because the v2 files are drawn
from the TypeScript compiler's services layer, which has a similar
ratio of declaration headers to body as the original fixture.
The variance is design-intended; the floor assertion is the contract
and all four languages pass it.

\paragraph{Body-leak canaries.} A separate test in the same harness
asserts that no function body bytes appear in the outline output: a
known sentinel string (\texttt{BENCH\_BODY\_SENTINEL\_DO\_NOT\_LEAK})
is placed in the test fixtures and the bench fails if it appears in
the compressed output. This is a strong cross-cutting guard that a
regression in the per-language extractor cannot silently bypass.

\subsection{Token-budget conformance}
\label{sec:eval:budget}

\Cref{fig:budget} reports measured conformance to the token budget
$B = 300$ on Phase 1a (Go-only corpus). Across all three repositories,
\textbf{59.3\%} of files stay within budget (600 files sampled,
200 per repo). Per-repository breakdown: Blackrim (jsgerman-oss/blackrim.dev@HEAD)
achieves \textbf{76.5\%} with a $p_{50}$ of 239~tokens; Go standard
library (golang/go@release-branch.go1.22) achieves \textbf{67.5\%}
with $p_{50} = 247$~tokens; Kubernetes
(kubernetes/kubernetes@release-1.30) achieves only \textbf{34.0\%}
with $p_{50} = 413$~tokens.

The Kubernetes result marks an empirical divergence from the design
assumption. Hand-written Go code (Blackrim, stdlib) meets or approaches
the 95\% target; codegen-heavy API types (Kubernetes \texttt{pkg/apis})
drive the outline past budget routinely, even after truncation of
nested private constructs. This is not a failure of the algorithm---the
truncation cascade (\cref{sec:algorithm:truncation}) is functioning
as designed---but a signal that the budget contract needs per-repo tuning
or the truncation heuristic needs refinement for codegen-dominated files.

We propose three remedies: (a) a per-repo budget override in
\texttt{ship.toml [outline] budget\_tokens} to allow operators to set
$B$ per corpus; (b) retention of the \texttt{outline-budget-exceeded}
log event (already instrumented in \cref{sec:system}) for inspection
of which file classes exceed budget most; (c) tightening the truncation
cascade to truncate public-declaration signatures (currently preserved)
when nested constructs fail to fit, rather than halting at the private-construct
boundary. Caveat: the outline token estimate uses the $\text{chars}/4$
approximation (\cref{sec:design:tokenizer}); actual counts via the
\texttt{cl100k} encoder vary by $\pm 5{-}15\%$ and are captured in the
raw telemetry.

\begin{figure}[ht]
\centering
\input{figures/budget-conformance}
\caption{Percentage of files staying within the 300-token outline budget,
by repository. Blackrim and Go stdlib (hand-written code) stay near the
95\% design target; Kubernetes (heavy API codegen) falls substantially
short. Dashed line marks the pre-registered 95\% success criterion;
observed data drives toward either per-repo budget overrides or
tighter truncation heuristics for public-declaration signatures.}
\label{fig:budget}
\end{figure}

\subsection{Outline emission latency}
\label{sec:eval:latency}

\Cref{fig:latency} reports measured warm-run latency across file sizes
on Phase 1a (Blackrim Go files only). The 500\,ms target is comfortably
met: median latency (warm $p_{50}$) ranges from \textbf{37.81\,ms}
on files of 100--200~LoC to \textbf{41.03\,ms} on files of 1000--2000~LoC,
an order of magnitude below target. The 95th-percentile warm $p_{95}$
ranges from 40.99\,ms to 42.67\,ms, still subsecond. This performance
profile reflects the dominant cost: tree-sitter AST parse and traversal.
No sample files in Phase 1a exceeded 2000~LoC; corpus extension to
the stdlib and Kubernetes corpora, and bucketing up to 5000~LoC, is
deferred to blackrim-d8ya (Phase 1b, Python/JS/TS).

The first-token latency (the user-perceived cost in the prepend-to-Read
path; \cref{sec:system:hook}) is not separately reported in this
measurement; it is one emission call among the warm runs and is
thus included in the $p_{50}$ and $p_{95}$ statistics. The overhead
of prepending a brief Markdown outline to a \texttt{Read} result is
amortized across the tool call and is negligible ($< 50$\,ms,
already within the $p_{95}$ bounds above).

\begin{figure}[ht]
\centering
\input{figures/latency-by-bucket}
\caption{Outline emission latency (warm runs) by file size bucket.
Median latency ($p_{50}$, blue) is 37--41\,ms across all buckets;
95th-percentile ($p_{95}$, orange) is 40--42\,ms. Empirically, the
design achieves sub-second latency on files up to 2000~LoC, well
below the 500\,ms target. Tree-sitter parse cost dominates; no
regression-inducing bottleneck has been observed. Deferred to Phase 1b:
latency measurements on Python/JS/TS files and file sizes up to 5000~LoC.}
\label{fig:latency}
\end{figure}

\subsection{Outline-discipline adoption trajectory}
\label{sec:eval:adoption}

\textbf{Pending --- OQ-AST-3.} The pre-registered hypothesis is that
the warn-mode-with-auto-prepend hook will drive adoption to $\geq
80\%$ within 14 days of rollout across all transcripts, sufficient to
trigger the promote-to-block trigger
(\cref{sec:system:discipline}). Empirically open:
\tothink{rolling 7-day hit-rate over the rollout period, plotted with
the 80\% promotion threshold marked; alongside, a histogram of
per-transcript hit-rate showing whether adoption is uniform across
agents or concentrated in a few well-disciplined ones.}

\subsection{Compile-gate false-negative rate}
\label{sec:eval:gate}

\paragraph{Finding: FP contract violated; FN rate zero.}
We present a preliminary measurement at $N{=}93$ corpus rows across
three intents (\emph{rename-symbol}, \emph{fix-imports},
\emph{extract-to-package}) and four languages (Go, Python, JavaScript,
TypeScript), drawn from the 40-file polyglot corpus in
\texttt{scripts/corpus-files/}.
The corpus is described by \texttt{scripts/refactor-corpus.yaml};
results are produced by \texttt{scripts/refactor-eval.py} and archived
in \texttt{data/scratch/refactor-eval-confusion.csv}.
\Cref{fig:gate-confusion} reports the confusion matrix.

\paragraph{False-positive rate (contract violation).}
\textbf{FP rate: 23.1\% on reject-expected rows in the
\texttt{extract-to-package}/Go cell (3 of 13 rows; Wilson 95\% CI
[8.2\%, 50.3\%]).}
This is a \emph{contract violation} of \cref{def:false_negative}:
the gate accepted diffs that ground-truth review would have rejected.

The three FP rows (\texttt{ep-go-fp-01..03}) are deliberate probes
that exercise the \textbf{blackrim-34qd} mechanism, which was
pre-discovered before this eval ran.  The mechanism is as follows.
When \texttt{extract-to-package} writes a newly-created Go package
that fails to compile --- for example, because the generated file has
a syntax error, a duplicate field declaration, or an undefined
reference to a symbol from the source package that was not imported ---
\texttt{go list -json .} exits non-zero for that directory.
\texttt{goPackageOfFile} (\texttt{cmd/gt/extract/compile\_gate\_scope.go},
\texttt{TouchedPackagesFromFiles}) treats a non-zero exit as a signal
to return the empty string, and the caller silently drops the package
from the touched-package set.  \texttt{RunCompileGate} is then called
with an empty \texttt{pkgs} slice, hits the early-return guard at
line~55, and returns a \texttt{CompileGateResult\{\}} with no errors.
The gate \emph{falsely passes}.

The §3.2 false-positive-free contract is currently \textbf{violated}
for the \texttt{extract-to-package}/Go intent when the new package
fails to compile.  This is an honest finding; we do not paper it over.
The fix is filed as \textbf{blackrim-34qd}: \texttt{goPackageOfFile}
must propagate the \texttt{go list} failure instead of returning
\texttt{""}, so that \texttt{TouchedPackagesFromFiles} can surface it
to the caller and the gate can reject the diff.  That fix is out of
scope for this measurement.\footnote{Fixed post-measurement in commit
\texttt{73f2f8ae} (PR~\#292, branch
\texttt{fix/compile-gate-fp-blackrim-34qd-v2}): three coordinated
changes --- (1)~\texttt{go list -e} to recover the import path even
for broken packages, (2)~the \texttt{build-output} action handled
explicitly in \texttt{parseBuildOutput}, (3)~\texttt{redeclared} and
\texttt{duplicate field} patterns added to \texttt{looksLikeBuildError}
--- reduce the FP rate to $0/93$ on the same corpus (re-run:
$\text{FP} = 0$, Wilson 95\%\ CI~$[0.0\%, 3.9\%]$).}

For all other eight (intent, language) cells, FP~$= 0$.

\paragraph{False-negative rate.}
\textbf{FN rate: 0.0\% (0 of 80 accept-expected rows accepted by gate;
Wilson 95\% CI [0.0\%, 4.6\%]).}
The gate rejected every diff that should be rejected (TN~$= 10$, across
all intents and languages) and passed every valid diff (TP~$= 80$).
The 0.0\% FN rate is well within the pre-registered 5\% conservatism
budget (\cref{sec:eval:gate:budget}); the upper bound of the 95\% CI
(4.6\%) also clears the budget.

\paragraph{Per-language and per-intent breakdown.}
\Cref{fig:gate-confusion} panel~(a) shows all nine FN cells at zero.
The TN cells (10 total) are: \texttt{rename-symbol}/Go (1),
\texttt{rename-symbol}/Python (1), \texttt{rename-symbol}/TypeScript (1),
\texttt{fix-imports}/Go (1), \texttt{fix-imports}/Python (1),
\texttt{extract-to-package}/Go (2, excluding the 3 FP probes),
\texttt{extract-to-package}/Python (2),
\texttt{extract-to-package}/TypeScript (1).
In each TN case the gate correctly detected an undefined reference,
a circular import, a stale call site, or a bad import removal and
rejected the diff.

\paragraph{Limitations.}\label{sec:eval:gate:limits}
\begin{enumerate}[leftmargin=*,nosep]
  \item \textbf{Corpus size.}  $N{=}93$ is a preliminary measurement.
        The pre-registered target was $N{=}200$; the corpus is labelled
        ``preliminary at $N{=}93$'' until it reaches that threshold.
        The Wilson CI upper bound on FN rate (4.6\%) is close to the
        5\% budget; a larger corpus is needed to establish the claim
        with tighter confidence.

  \item \textbf{FP source localization.}  The three FPs are not a
        random sample of the FP failure mode --- they are deliberate
        probes of one specific mechanism (blackrim-34qd).  The true
        FP rate in production over a wider corpus of \emph{unprobed}
        rows is 0/90 in this measurement, consistent with the
        intended-zero contract.  The probes are marked
        \texttt{is\_fp\_probe: true} in the corpus YAML and
        \texttt{is\_contract\_violation: true} in the results CSV.

  \item \textbf{No live gate invocation.}  The corpus rows record
        hand-annotated \texttt{gate\_verdict} values derived from
        static analysis of the code path, not from live \texttt{gt}
        invocations.  A live-invocation harness is deferred to
        Phase~1b; the current measurement is a pre-registered
        contract-violation check, not an end-to-end run.
\end{enumerate}

\input{figures/gate-confusion}

\subsection{End-to-end token reduction on real agent traces}
\label{sec:eval:e2e}

% ---------------------------------------------------------------------------
% Headline-statistic macros.  These carry "TBD" until fill-paired-trace-macros.py
% is run against the scored figures-data/*.json files from the live eval.
% They render literally as "TBD" in the PDF during the pending-data phase,
% which is correct and explicit.  Replace with numbers post-eval by running:
%   python3 scripts/fill-paired-trace-macros.py
% ---------------------------------------------------------------------------
\newcommand{\headlineDeltaTokens}{TBD}
\newcommand{\headlineDeltaTokensCI}{TBD}
\newcommand{\successProportionAtLeastZero}{TBD}
\newcommand{\successProportionCI}{TBD}

\paragraph{Pre-registered comparison.}
The central pending claim for this paper is that outline-first reading
reduces the \emph{total} input-token cost across a real agent task ---
not just per-file (\cref{sec:eval:compression}) --- while preserving
downstream task success.  The design already shows that individual files
compress by 65--78\% depending on language; the integrated question is
whether that savings survives the full task lifecycle, where an agent may
read some file bodies after their outlines, pays hook overhead, and issues
turns that do not touch file reads at all.

The pre-registered comparison is: \textbf{paired traces} of the same
Explore-tier tasks under (a)~\emph{baseline}: no hook, agents read files
in full; (b)~\emph{outline-discipline enabled}: the production
\texttt{outline-discipline.sh} PreToolUse hook in block-mode enforces
outline-before-read for all files $\geq 200$~LoC.  Both conditions use
the same agent, the same task prompt, and the same model tier; only the
runtime hook differs (design rationale:
\texttt{docs/design/paired-trace-harness.md}~\S3.3).
Outcome metrics: weighted per-task input tokens (cash price per
Anthropic's pricing model at experiment time), wall-clock to first
answer, and per-task success as measured by a per-corpus goal predicate.

\paragraph{Headline statistics (pending data).}
The median per-task input-token reduction is
\textbf{\headlineDeltaTokens} (95\%~BCa CI \headlineDeltaTokensCI),
computed via paired BCa bootstrap over $N{=}50$ tasks with $K{=}3$
inner runs each (1000 replicates, seed 20260520;
\texttt{docs/design/paired-trace-harness.md}~\S8.4).
The proportion of tasks where outline-discipline preserved or improved
success is \textbf{\successProportionAtLeastZero}
(95\%~Wilson CI \successProportionCI).
\Cref{fig:paired-tokens-distribution} shows the per-task
$\Delta_\text{tokens}$ distribution;
\Cref{fig:paired-success-distribution} shows the
$\Delta_\text{success} \in \{-1, 0, +1\}$ task counts.

\begin{figure}[ht]
\centering
\input{figures/paired-tokens-distribution}
\caption{Per-task paired token-delta distribution
($\Delta_\text{tokens} = W_\text{on} - W_\text{off}$; negative values
indicate savings).  Weighted input tokens count cache-read tokens at
10\% per Anthropic's published pricing.  Median and 95\%~BCa CI
annotated.  \textit{Pending:} figure renders a ``Data pending''
placeholder until the 300-trace eval
(50 tasks $\times$ 2 conditions $\times$ $K{=}3$ inner runs) executes.}
\label{fig:paired-tokens-distribution}
\end{figure}

\begin{figure}[ht]
\centering
\input{figures/paired-success-distribution}
\caption{Per-task success delta
($\Delta_\text{success} \in \{-1, 0, +1\}$) task counts.
$\Delta_\text{success} = \operatorname{sign}(S_\text{on} - S_\text{off})$
where $S_{\cdot}$ is the median success indicator over $K$ inner runs for
that condition.  Bars show count of tasks where outline-discipline
degraded, preserved, or improved success.  Wilson 95\%~CI on
$\Pr(\Delta_\text{success} \geq 0)$ annotated.
\textit{Pending:} placeholder until eval executes.}
\label{fig:paired-success-distribution}
\end{figure}

\paragraph{Confound handling.}
Four highest-impact mitigations are in place, per
\texttt{docs/design/paired-trace-harness.md}~\S9:
\begin{enumerate}[leftmargin=*,nosep]
  \item \textbf{Ordering randomisation.}  For each (task, inner-run)
        pair, the order of ON and OFF conditions is randomised (coin
        flip, recorded in \texttt{order\_in\_pair}).  This cancels
        first-order prompt-cache-warming effects: within a pair, the
        \emph{second} run pays cache-read rates regardless of condition;
        the randomisation ensures this advantage is distributed evenly
        across ON and OFF.

  \item \textbf{Model-id pin.}  The harness reads the Anthropic model
        release ID at the first run and aborts if it shifts mid-eval.
        The entire 300-trace dataset is collected within a $<72$-hour
        window to stay within a single model-stability period.

  \item \textbf{Nonce cache-busting.}  Each run's prompt appends a
        unique \texttt{[NONCE:~<random-hex>]} line, guaranteeing the
        per-task prompt is unique across all 300 runs and defeating
        Anthropic-side prompt-cache hits on the task content.  The
        CLAUDE.md prefix is intentionally \emph{not} cache-busted; it
        is identical in production and the cache hit is valid.

  \item \textbf{Golden-contamination probes.}  Five corpus tasks
        exercise recently-renamed symbols (a \texttt{rename-symbol}
        transform within the preceding 7~days).  If an agent answers
        correctly with 0 file reads, the task is flagged as contaminated
        and excluded; otherwise contamination is treated as cancelled by
        the paired design ($\Delta_\text{success}$ is 0 on both
        contaminated conditions).
\end{enumerate}

\paragraph{Limitations.}\label{sec:eval:e2e:limits}
\begin{enumerate}[leftmargin=*,nosep]
  \item \textbf{Sample size.}  $N{=}50$ tasks with $K{=}3$ inner runs
        is a preliminary measurement by the standards of behavioural
        HCI work, though the power analysis
        (\texttt{docs/design/paired-trace-harness.md}~\S8.6) shows
        it is sufficient to detect a 30\% relative reduction at
        $\alpha{=}0.05$, $\beta{=}0.20$ under the empirical per-file
        variance from \cref{sec:eval:compression}.  A follow-up with
        $N \geq 100$ tasks is pre-registered as
        \texttt{blackrim-wy1l}~Phase~2.

  \item \textbf{Drop rate ceiling.}  Runs that time out or produce
        malformed output are retried up to three times; beyond that,
        the task is dropped.  If the drop rate exceeds 5\% of the
        50-task corpus, results are flagged as preliminary pending a
        re-run.

  \item \textbf{Cache-warming within a task pair.}  The nonce
        cache-buster neutralises per-task prompt cache hits; it does
        not eliminate \emph{cross-task} cache effects on the CLAUDE.md
        prefix (which we explicitly allow, as it mirrors production).
        The harness logs \texttt{cache\_creation\_input\_tokens} and
        \texttt{cache\_read\_input\_tokens} per turn so reviewers can
        verify direction and magnitude of any residual bias.

  \item \textbf{Public-corpus contamination.}  Tasks drawn from
        \texttt{django/django} and \texttt{kubernetes/kubernetes} are
        on public corpora that may be in Anthropic's training data.
        Five golden-contamination probes check the most likely failure
        mode; if $\geq 2$ of the 5~probes detect contamination, the
        paper rescopes the headline statistics to the Blackrim-only
        portion of the corpus ($n{=}25$ tasks) and notes the limitation
        prominently.

  \item \textbf{Currently-pending data.}  At submission time, the
        eval had not yet been executed.  The experimental design,
        harness, and scoring chain are fully in place across the
        following \texttt{blackrim-wy1l} sub-issues:
        \texttt{blackrim-0dvz}~(F2, trace capture);
        \texttt{blackrim-rr7t}~(F3a, replay harness Stage~1);
        \texttt{blackrim-pdvn}~(F3b, scoring Stage~2);
        \texttt{blackrim-qz9n}~(F5, bootstrap + figure data);
        \texttt{blackrim-2baq}~(F4a, corpus assembly);
        \texttt{blackrim-8927}~(F4b, golden contamination probes).
        The placeholder macros \verb|\headlineDeltaTokens|,
        \verb|\headlineDeltaTokensCI|,
        \verb|\successProportionAtLeastZero|, and
        \verb|\successProportionCI| will be populated by
        \texttt{scripts/fill-paired-trace-macros.py} once the scored
        dataset lands.  All other structure of this section is final.
\end{enumerate}

\subsection{Reproducibility}
\label{sec:repro}

Every quantitative claim in this section is backed by a CSV in
\texttt{data/} produced by a script in \texttt{scripts/}. The full
reproduction pipeline is:

\begin{lstlisting}[language=bash]
# Reproduces 6.2 (currently measured; uses local corpus-files/ corpus)
python scripts/pull-compression-ratios.py \
    > data/raw/compression-ratios.jsonl
python scripts/aggregate-by-language.py \
    < data/raw/compression-ratios.jsonl \
    > data/aggregated/by-language.csv
# Legacy n=1 single-fixture mode (pre-v2):
#   python scripts/pull-compression-ratios.py --legacy \
#       --blackrim-root=$BLACKRIM_ROOT > ...

# Reproduces 6.4 (pending; harness ready)
python scripts/measure-outline-latency.py \
    --blackrim-root=$BLACKRIM_ROOT \
    > data/aggregated/outline-latency.csv

# Reproduces 6.5 (pending; needs accumulated telemetry)
python scripts/pull-outline-telemetry.py \
    --telemetry=$BLACKRIM_ROOT/.beads/telemetry/outline-events.jsonl \
    > data/aggregated/outline-events.csv

make
\end{lstlisting}

The pipeline is deterministic given (a) a Blackrim checkout at a
fixed commit and (b) the test corpus pinned in
\texttt{scripts/corpus.txt}. We anchor the published version to a
tagged Blackrim commit; the companion repository
\cite{blackrim_paper_repo} hosts the artefacts.
