\section{Conclusion}
\label{sec:conclusion}

% 1. What we did. Frequency-weighted progressive disclosure framework;
%    applied to Blackrim's CLAUDE.md; 21.5% line reduction without
%    instruction-fidelity loss; integrated literature review showing
%    why compression is risky and KGs are wrong-tool for instruction
%    docs.
%
% 2. What this is not. We did not show learned compression is bad on
%    instructions; we showed it is unmeasured. We did not show our
%    methodology generalises beyond CLAUDE.md; we showed it works on
%    one large-static-prefix multi-agent system.
%
% 3. Why this matters. Static-prefix multi-agent coding systems are a
%    growing pattern. The cost shape is dominated by prompt-cache
%    amortisation, but maintenance and human-onboarding scale with
%    line count. A measurement-first methodology keeps both axes
%    healthy.
%
% 4. Open questions for the field. A held-out instruction-following
%    eval suite for static prefixes; benchmarks for learned
%    compression on instruction documents; cache-control exposure
%    in client tooling.

\tothink{Write conclusion prose; target $\sim$0.5 page.}
